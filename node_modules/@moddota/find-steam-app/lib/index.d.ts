import { AppManifest } from './manifest';
import { findSteam } from './steam';
export { AppManifest, findSteam };
export declare class SteamNotFoundError extends Error {
    constructor();
}
/**
 * Searches for all local Steam libraries.
 *
 * @returns Array of paths to library folders.
 */
export declare function findSteamLibraries(): Promise<string[]>;
/**
 * Searches for app in local Steam libraries.
 *
 * @returns Information about installed application.
 */
export declare function findSteamAppManifest(appId: number): Promise<AppManifest | undefined>;
/**
 * Searches for app in local Steam libraries.
 *
 * @returns Path to installed app.
 */
export declare function findSteamAppByName(name: string): Promise<string | undefined>;
/**
 * Searches for app in local Steam libraries.
 *
 * @returns Path to installed app.
 */
export declare function findSteamAppById(appId: number): Promise<string | undefined>;
//# sourceMappingURL=index.d.ts.map